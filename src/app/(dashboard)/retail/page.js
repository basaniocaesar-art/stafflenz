import DashboardPage from '@/components/DashboardPage';
export const metadata = { title: 'Retail Dashboard — LenzAI' };
export default function RetailDashboard() {
  return <DashboardPage industry="retail" />;
}
