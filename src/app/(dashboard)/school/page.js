import DashboardPage from '@/components/DashboardPage';
export const metadata = { title: 'School Dashboard — LenzAI' };
export default function SchoolDashboard() {
  return <DashboardPage industry="school" />;
}
